# 2026-08-24 工作记录：安全评估 + 加固执行

## 背景

对公网服务器 186.241.89.117（zcmwxy.duckdns.org）做安全核查并执行加固计划。

## 核查结论（实测）

已生效的旧加固：1337/3000/5432 安全组已关；HTTPS 已上线（Let's Encrypt，证书 2026-08-23 签发，11-21 到期）；安全响应头齐全；泄露的 CMS 管理员密码已轮换（实测 Invalid credentials）；root 密码已轮换（git 历史中的旧密码实测认证失败）。

仍存在的问题：**Strapi /admin 公网可达**（HTTP/HTTPS 均 200）；**SSH 仍接受密码认证**（publickey,password，云厂商 cloud-init 覆盖）；HTTP 80 未 301；nginx/Next 版本暴露；仓库历史含泄露密码（GitHub Public）。

## 已执行（本地，全部完成）

1. 清除文档明文凭据：`.workbuddy/memory/2026-08-20.md`、`deploy/LAN_DEPLOYMENT_RECORD.md` 中 4 个密码全部替换为占位符
2. `git rm --cached deploy/ssh_exec.py ssh_connect_test.py`，补 `.gitignore`（含根目录脚本、__pycache__）
3. 新增 `deploy/harden_server.py` 一键加固脚本（幂等 + 自保护 + 本地单测 13 项全过，测试在 `deploy/_test_harden_local.py`）
4. 依赖加固：frontend next 16.2.10→**16.3.2**（修 high 漏洞），npm audit 生产依赖 **0 漏洞**；lint/tsc/build 三项验证通过。cms 的 audit fix 因 better-sqlite3 Windows 编译失败跳过（多为 dev 工具链漏洞，待后续在 Linux 侧处理）
5. git 历史清理：filter-repo 替换 4 个密码 → force push（备份 `../gaoxiaoweb-backup-20260824.bundle`）

## 未执行（需服务器凭据，用 harden_server.py 一键完成）

在项目根目录建 `.env`（参照 .env.example，填 SSH_HOST/SSH_USER/SSH_PASSWORD 或 SSH_KEY），然后：

```bash
pip install paramiko
python deploy/harden_server.py --dry-run   # 先看 diff
python deploy/harden_server.py             # 全部执行
```

脚本覆盖：/admin 拦截、80→301、限流、隐藏版本、**PG 密码轮换**（打印一次新密码）、fail2ban、certbot 验证、每日备份+证书监控、unattended-upgrades、**SSH 禁密码**（先部署密钥并验证成功才禁，不会锁死）。

## 重构：最简版纯静态架构（同日第二项工作）

用户确认内容改为 git/文件维护后，删除 Strapi CMS 重构为纯静态站点：

- 删除 `cms/`（整个 Strapi）、`frontend/lib/cms.ts`、`lib/security-headers.ts`、旧部署文档（SERVER_DEPLOYMENT.md / LAN_DEPLOYMENT_RECORD.md，git 历史可查）
- 新增 `frontend/content/*.json`（7 个文件，ViewModel 结构）：site/articles/notices/banners/departments/faculty/pages，演示内容
- 新增 `frontend/lib/content.ts`：与原 cms.ts 同名同签名 API，读本地 JSON；页面组件零语义变化
- 静态导出适配：`output: "export"`；`/news` `/notices` `/faculty` `/organization` `/search` 的筛选/分页/搜索改为客户端组件（`NewsFilterableList` 等 + `useSearchParams` + Suspense）；feed.xml/sitemap/robots 加 `force-static`
- 新增部署工件：`deploy/campus-static.conf`（Nginx 静态托管+全套安全头+限流+缓存）、`deploy/deploy_static.py`（本地构建→SFTP 上传→releases/软链切换→装配置→验证，支持 `--shutdown-services`）
- 验证：lint 0 error、tsc 通过、build 30/30 页面全静态（out/ 共 3.9MB）

服务器迁移待办（需 .env 凭据）：
1. `python deploy/deploy_static.py --shutdown-services`（首次迁移：发布静态版+停用 campus-frontend/campus-cms+移除旧反代）
2. 之后可卸载 Node.js/PostgreSQL，服务器只跑 Nginx
3. harden_server.py 的 pg/backupcron 步骤在纯静态后不再需要（无数据库）

## 待办（手动，沿自上一节）

- GitHub 仓库转 Private：Settings → General → Danger Zone → Change visibility
- harden_server.py 若未跑：nginx/ssh/fail2ban/cert 步骤仍适用，pg 步骤跳过

