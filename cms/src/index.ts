/**
 * Strapi 应用入口 - src/index.ts
 * 
 * 功能：Strapi 应用生命周期钩子
 * 说明：POC 阶段无自定义启动逻辑
 */

export default {
  /**
   * 应用注册时调用（可选）
   * 
   * 在此可注册自定义中间件、服务、策略等
   */
  // register(/*{ strapi }*/) {},

  /**
   * 应用启动时调用（可选）
   * 
   * 用于执行初始化任务
   */
  // bootstrap(/*{ strapi }*/) {},

  /**
   * 应用销毁时调用（可选）
   * 
   * 用于清理资源、关闭连接等
   */
  // destroy(/*{ strapi }*/) {},
};
