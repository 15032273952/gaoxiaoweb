# SSH 远程部署连接信息（复制为 .env 并填写真实值；.env 已被 .gitignore 忽略，不会提交）
# 两个脚本会自动加载项目根目录的 .env，直接运行即可，无需手动 export
#
# 测试阶段：服务器密码登录与密钥登录均已开启
SSH_HOST=
SSH_PORT=22
SSH_USER=root
SSH_PASSWORD=
# 私钥路径（可选，与密码二选一，同时配置时密钥优先）
# SSH_KEY=
