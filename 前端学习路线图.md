# 前端学习路线图（Next.js + React + TypeScript 项目实战版）

> 定位：非前端背景，但要维护一个「Next.js App Router + TypeScript + Tailwind + CMS 取数」的项目。
> 目标不是成为前端专家，而是**尽快能看懂并动手改项目代码**。
>
> 总体路径：环境工具 → JS 基础 → 异步 → JSX → TS 类型标注 → React 组件与 Hooks → Next.js 路由与取数 → Tailwind 样式 → 读懂构建流程。
> 按每天 2 小时估算，约 2 周可以开始独立改项目。

---

## 学习顺序总览

```
必须亲手写代码练            边干边学，遇到再查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
JS 基础语法 → JS 异步 → JSX → TS 类型标注 → React Hooks
             → Next.js 路由与取数 → Tailwind 类名
```

一句话：**先把 JavaScript 的基础语法（变量、对象、数组、函数、async/await）吃透，然后直接上手改项目**。React、TypeScript、Next.js 的细节都是遇到了再查，不需要提前全部学完。

---

## 第〇部分：环境与工具（先做，约半天）

这一部分不写业务代码，但缺了寸步难行。

- [ ] 安装 Node.js（LTS 版本），理解 `npm install` 是干什么的、`package.json` 是什么
- [ ] 终端基本操作：`cd`、`ls`、`npm run dev` 能把项目跑起来，浏览器看到页面
- [ ] VS Code 装好插件：ESLint、Prettier、Tailwind CSS IntelliSense
- [ ] Git 基础：`clone` / `pull` / `add` / `commit` / `push`、切分支、出冲突时先 `pull` 再改
- [ ] 搞懂项目怎么启动、改完代码浏览器会不会自动刷新（Next.js dev 模式会热更新）

✅ 达标标准：本地能把项目跑起来，能看到页面，能正常拉代码、提交代码。

---

## 第一部分：JavaScript 基础（第一优先级，约 4~5 天）

这是地基，必须亲手写代码练习，不能只看不练。

### 1.1 变量声明

- [ ] `let` / `const`：默认用 `const`，只有值需要改变时才用 `let`，永远不要用 `var`
- [ ] 理解 `const obj = {}` 之后 `obj.name = 'a'` 是合法的（const 锁的是引用，不是内容）

### 1.2 数据类型

- [ ] 基本类型：字符串 string、数字 number、布尔 boolean、`undefined`、`null`
- [ ] 引用类型：对象 object、数组 array
- [ ] 假值（falsy）：`''`、`0`、`null`、`undefined`、`NaN` 在条件判断里都算 false——这个直接影响条件渲染

### 1.3 对象操作

- [ ] 取值：点语法 `obj.name` 和方括号 `obj['name']`
- [ ] 遍历：`Object.keys()` / `Object.values()` / `Object.entries()`
- [ ] **解构赋值**：`const { title, author } = article`（项目里到处都是这个写法，必须熟练）
- [ ] **可选链**：`obj?.author?.name`（CMS 返回的数据经常有字段缺失，这个是保命符）
- [ ] **空值合并**：`title ?? '默认标题'`，以及它和 `||` 的区别

✅ 达标标准：能安全地取出 CMS 返回的多层嵌套字段，不担心某一层是 null 导致报错。

### 1.4 数组操作

- [ ] `.map()`：遍历列表生成新数组（渲染列表全靠它）
- [ ] `.filter()`：过滤数据
- [ ] `.find()`：找到第一个满足条件的元素
- [ ] `.some()` / `.every()`：判断是否存在/全部满足条件
- [ ] 展开运算符 `[...arr]`（复制、合并数组）
- [ ] `.sort()` 排序、`.reduce()`（可以稍后再学）

✅ 达标标准：给一个文章数组，能按字段过滤、排序、提取标题列表。

### 1.5 条件判断

- [ ] `if/else`、`switch`
- [ ] 三元运算符：`条件 ? 真 : 假`（JSX 里条件渲染大量使用）

### 1.6 函数

- [ ] `function` 声明 vs 箭头函数 `() => {}`
- [ ] 默认参数 `function f(a = 1)`、解构参数 `f({ title })`
- [ ] 回调的概念：`.map(item => ...)` 就是把函数传进函数
- [ ] 箭头函数的 `this` 差异：概念了解即可，初期不深究

✅ 达标标准：能熟练读写箭头函数——项目里 90% 的函数都是箭头函数。

### 1.7 模块化（补充）

- [ ] `import` / `export` 的用法
- [ ] 默认导出 `export default` vs 命名导出 `export const xxx` 的区别（对应 import 时加不加大括号）

✅ 达标标准：能新建一个文件导出函数，在另一个文件里成功 import 使用。

### 1.8 异步（重中之重）

项目里从 CMS 取数据全靠它，必须重点攻克。

- [ ] 为什么需要异步：网络请求需要时间，不能阻塞
- [ ] Promise 的三种状态（pending / fulfilled / rejected）、`.then()` 链——能读懂即可
- [ ] `async/await`：`async` 函数里才能用 `await`，`await` 会等结果返回
- [ ] `fetch()` 请求 + `res.json()` 解析
- [ ] `try/catch` 错误处理：CMS 请求失败时怎么办
- [ ] `Promise.all` 并行请求（可以稍后学）

```js
// 项目里最常见的模式，要能默写出来：
async function getArticles() {
  try {
    const res = await fetch(CMS_URL);
    const data = await res.json();
    return data.articles;
  } catch (error) {
    console.error('获取文章失败', error);
    return [];
  }
}
```

常见坑：忘记写 `await`；在非 async 函数里用 `await`；忘记 `res.json()` 也是异步的。

✅ 达标标准：能独立写出「从某个 URL 拉取文章列表并处理失败」的完整代码。

### 第一阶段自测题

1. 给一个文章数组，过滤出 `published === true` 的文章，按发布时间排序，输出标题列表（filter + sort + map 链式调用）。
2. 写一个 async 函数，请求失败时返回空数组而不是崩溃。
3. 用解构 + 可选链从 `data?.article?.author?.name` 安全取值并给默认值。

---

## 第二部分：JSX 语法（约 1 天）

JSX 是 React 的模板语言，本质是「在 JS 里写 HTML」，最终会被编译成函数调用。

- [ ] 标签里写变量/表达式：`<h1>{title}</h1>`（大括号里能放任何 JS 表达式）
- [ ] 条件渲染：`{isVisible && <div>可见</div>}`、三元运算符 `{a ? <X/> : <Y/>}`（注意：JSX 里不能直接写 `if`）
- [ ] 列表渲染：`{articles.map(a => <ArticleCard key={a.id} {...a} />)}`
- [ ] `key` 必须唯一且稳定，一般用 id，不要用数组下标
- [ ] 属性差异：`class` → `className`、`for` → `htmlFor`
- [ ] 行内样式：`style={{ color: 'red' }}`（双层大括号）
- [ ] 事件绑定：`onClick={() => setOpen(true)}`
- [ ] Fragment：`<>...</>`（需要返回多个并列标签时用）

✅ 达标标准：能写出一个卡片组件的完整结构，把标题、图片、标签数据嵌进去，并实现「没有封面图时不显示图片区域」。

---

## 第三部分：TypeScript 类型标注（约 1 天）

- [ ] 基本标注：`: string`、`: number`、`: boolean`
- [ ] 联合类型：`string | null`（可能是字符串也可能是 null）
- [ ] 字面量联合：`'draft' | 'published'`（CMS 的状态字段常用）
- [ ] 数组类型：`Article[]`
- [ ] **接口 interface**：给对象定义形状——项目的 `lib/types.ts` 里全是这个，是重点
- [ ] 可选属性：`name?: string`（问号表示可选）
- [ ] `import type { Article } from './types'`
- [ ] 学会读 TS 报错：一般看第一行，它会告诉你哪个字段对不上

```ts
// 项目里典型的类型定义，要能照着写：
interface Article {
  id: string;
  title: string;
  summary?: string;          // 可选
  coverImage: string | null; // 联合类型
  publishedAt: string;
  tags: string[];
}
```

进阶内容先不深究：泛型、`Partial` / `Pick` 等工具类型——混个脸熟，遇到再查。

✅ 达标标准：CMS 新增一个字段时，你能在 `types.ts` 里补上对应类型；看到 TS 红色报错不慌，能读懂它在抱怨什么。

---

## 第四部分：React 核心概念（约 2~3 天）

### 4.1 组件思维

- [ ] 组件 = 返回 JSX 的函数
- [ ] 组件父子关系：`layout.tsx` → `page.tsx` → `ArticleCard.tsx` 的嵌套结构
- [ ] Props 传参：父组件把数据通过 props 传给子组件，数据单向流动
- [ ] `{...a}` 展开传参的含义

### 4.2 React Hooks（客户端组件才用）

- [ ] `useState`：处理用户交互状态（开关菜单、点击展开）；改状态必须用 setter，不能直接改变量
- [ ] `useEffect`：处理副作用（页面加载时获取数据、监听事件）；理解依赖数组 `[]` 的含义和清理函数
- [ ] 什么时候需要 `'use client'`：需要 state、事件监听、浏览器 API 时

```jsx
'use client';
import { useState } from 'react';

export default function Menu() {
  const [open, setOpen] = useState(false);
  return <button onClick={() => setOpen(!open)}>{open ? '关闭' : '打开'}</button>;
}
```

✅ 达标标准：能独立写一个「点击展开/收起」的交互组件。

### 4.3 Server Component（项目里的默认情况）

- [ ] 概念：代码在服务端/构建时运行，可以直接 `await` 取数据，不需要 `'use client'`
- [ ] 理解为什么 `page.tsx` 里可以直接写 `const articles = await getArticles()`
- [ ] Server / Client 组件的边界：数据获取放服务端，交互放客户端

✅ 达标标准：能向别人解释「为什么页面组件里能直接 await 取数据」。

---

## 第五部分：Next.js App Router（约 2 天）

- [ ] 文件系统路由：`app/about/page.tsx` 对应 URL `/about`
- [ ] `layout.tsx`：嵌套布局，所有子页面共享的头部/导航放这里
- [ ] `metadata` 导出：页面标题、描述（SEO 用）
- [ ] **动态路由**：`app/articles/[slug]/page.tsx`，通过 `params.slug` 拿到 URL 参数
- [ ] `generateStaticParams`：构建期预生成所有文章详情页（项目大概率用了，要读懂）
- [ ] `<Link>` 组件做页面跳转（不要用 `<a>`）
- [ ] `loading.tsx` / `error.tsx` / `not-found.tsx`：能读懂即可
- [ ] **环境变量**：`.env.local` 文件、`process.env.XXX` 读取、`NEXT_PUBLIC_` 前缀的含义（客户端能访问的变量才加这个前缀）
- [ ] 常用命令：`next dev`（开发）、`next build`（构建）、`next start`（生产运行）
- [ ] 构建报错时会指出具体文件和行号，学会顺着报错找问题

✅ 达标标准：能自己新增一个页面；能解释文章详情页的 `[slug]` 是怎么来的、数据是怎么拿到的。

---

## 第六部分：CSS 基础（Tailwind，约 1~2 天，边用边学）

- [ ] 工具类思维：类名直接写样式——`p-4` 内边距、`text-xl` 字号、`bg-blue-500` 背景色、`rounded-lg` 圆角
- [ ] 间距规律：1 个单位 = 0.25rem，`p-4` = 1rem
- [ ] **flex 布局（最常用）**：`flex`、`items-center` 垂直居中、`justify-between` 两端对齐、`gap-4` 间距
- [ ] grid 基础：`grid grid-cols-3 gap-4`
- [ ] 响应式前缀：`md:`、`lg:`（移动端优先，`md:flex-row` 表示中屏以上才横排）
- [ ] 状态类：`hover:bg-blue-600`、`focus:`、`dark:`
- [ ] 条件类名拼接：模板字符串或 `clsx` 库
- [ ] 项目的主题色：`--thu-purple` 等 CSS 变量，看 `globals.css` 里的定义和用法
- [ ] 配合浏览器 DevTools 的 Elements 面板实时调试样式

✅ 达标标准：能调整 `ArticleCard` 的间距和布局；能改出「手机上单列、桌面上三列」的响应式效果。

---

## 第七部分：概念性了解（能读懂就行，不需要会写）

- [ ] 构建期取数流程：`next build` → 从 CMS 拉数据 → 渲染成静态 HTML → 部署
- [ ] 组件树的数据流向：数据在哪一层获取，通过 props 一路传下去
- [ ] `node_modules`（依赖包）、`.next`（构建产物）是什么，都不要手动改
- [ ] 部署流程的大致环节

---

## 第八部分：通用能力（常被忽略但极其重要，补充）

- [ ] 浏览器 DevTools：Console 看报错、Network 看请求是否成功、Elements 看样式
- [ ] `console.log` 调试大法：不确定数据结构就先打印出来看
- [ ] 读报错信息：先读第一行，复制关键信息去搜索或问 AI
- [ ] 高效提问的格式：贴上报错信息 + 相关代码 + 你想达到的效果

---

## 两周学习计划（参考，每天约 2 小时）

| 天数 | 内容 | 产出 |
| --- | --- | --- |
| 第 1 天 | 环境搭建 + Git + 把项目跑起来 | 本地能看到项目页面 |
| 第 2~5 天 | JS 基础语法 + 异步，每天必须手写代码 | 完成第一阶段自测题 |
| 第 6 天 | JSX | 手写一个卡片组件 |
| 第 7 天 | TypeScript 类型标注 | 给模拟数据写出 interface |
| 第 8~9 天 | React 组件 + Hooks | 写出展开/收起交互组件 |
| 第 10~11 天 | Next.js 路由与取数 | 读懂项目的路由和取数链路 |
| 第 12 天 | Tailwind | 调整一个现有组件的布局 |
| 第 13~14 天 | 项目实战 | 完成下方练习任务 1~3 |

## 实战练习任务（按难度递增，直接在自己的项目上练）

1. **改文字**：修改某个页面的标题文案和颜色类名——熟悉项目文件定位和 Tailwind。
2. **改样式**：调整 `ArticleCard` 的间距、排版——熟悉 flex 与间距类。
3. **加字段显示**：在卡片上多显示一个 CMS 字段——串联 `types.ts` + JSX + 样式。
4. **条件渲染**：某字段为空时显示占位样式——熟悉 `&&` 与三元渲染。
5. **新增静态页面**：加一个 `/about` 页面——熟悉路由与 layout。
6. **加交互**：给列表加一个「展开更多」按钮——熟悉 `useState` + `'use client'`。

## 学习方法建议

- 教程看一遍就够，剩下时间全部用来**亲手写代码**。
- 每次只改一小块，马上运行看效果，建立即时反馈。
- 遇到不懂的先看报错、再查文档、再问 AI——但问完之后**自己敲一遍**。
- 不要追求学完再动手：第一部分学完就可以开始改项目里最简单的文字和样式了。

## 推荐资料（够用了，别贪多）

- MDN Web Docs（JavaScript 语法查询的权威来源）
- React 官方教程（react.dev 的 Learn 部分，有中文）
- Next.js 官方交互式教程（nextjs.org/learn，讲 App Router 最清楚）
- Tailwind CSS 官方文档（配合 VS Code 插件的悬浮提示，边写边学最快）
